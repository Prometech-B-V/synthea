services:
  api:
    build:
      context: .
    image: ghcr.io/prometech-b-v/synthea:palaestra-latest
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xms512m -Xmx2g
    restart: unless-stopped

